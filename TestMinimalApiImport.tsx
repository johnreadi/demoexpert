import React from 'react';
import * as api from './api';

export default function TestMinimalApiImport(): React.ReactNode {
  return (
    <div>
      <h1>Test Minimal API Import</h1>
      <p>If this component renders without errors, the API import is working correctly.</p>
    </div>
  );
}