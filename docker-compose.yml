<<<<<<< HEAD
services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: always
    environment:
      NODE_ENV: production
      PORT: 8080
      SESSION_SECRET: ${SESSION_SECRET}
      TRUST_PROXY: "1"
      COOKIE_SECURE: "true"
      COOKIE_SAME_SITE: lax
      CORS_ORIGIN: https://app.demoexpert.fr
      DATABASE_URL: ${DATABASE_URL}
    expose:
      - "8080"
    command: |
      sh -c "
        for i in $(seq 1 60); do nc -z expert-dbexpert-nh7w6a 5432 && echo 'database reachable' && break || echo 'waiting for database...' && sleep 3; done;
        npm run db:generate &&
        npx prisma migrate deploy || true &&
        npm run db:seed || true &&
        node dist/index.js
      "
    networks:
      - traefik_net
      - overlay_net
    labels:
      - traefik.enable=true
      - traefik.http.routers.demoexpert-api.rule=Host(`app.demoexpert.fr`) && PathPrefix(`/api`)
      - traefik.http.routers.demoexpert-api.entrypoints=websecure
      - traefik.http.services.demoexpert-api.loadbalancer.server.port=8080
      - traefik.http.routers.demoexpert-api.tls.certresolver=letsencrypt
=======
version: '3.8'
>>>>>>> 54337d572bdc5a1ee2408b3fd73abe2e90ad38fa

services:
  expert-app:
    image: expert-frontend-5ftp7oiaest:latest
    container_name: expert_app
    restart: unless-stopped
    networks:
      - traefik_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.expert.rule=Host(`app.demoexpert.fr`)"
      - "traefik.http.routers.expert.entrypoints=websecure"
      - "traefik.http.routers.expert.tls=true"
      - "traefik.http.routers.expert.tls.certresolver=letsencrypt"
      - "traefik.http.services.expert.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 5s
      retries: 3

networks:
  traefik_net:
    external: true
    name: dokploy_dokploy-network
  overlay_net:
    external: true
    name: app-shared-net
