services:
  web:
    build: .
    container_name: demolition-expert-web
    restart: always
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - API_KEY=${API_KEY}
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
